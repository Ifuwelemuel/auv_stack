/depth_sensor:
  ros__parameters:
    i2c_bus: 1              # Pi header I2C, 
    fluid_density: 997.0    # kg/m^3 — fresh water. 
    rate_hz: 10.0
    max_failures: 5         # consecutive read failures before sensor declared dead